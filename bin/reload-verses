#!/bin/sh

echo "Reload ... verses"
if [ $( uname -s ) == "Darwin" ]; then
  find ${RENGUPATH}/verses -type f | \
  xargs -n100 rengu load verse > ${RENGUPATH}/check/verses.load
else
  rengu load verse ${RENGUPATH}/verses/* > ${RENGUPATH}/check/verses.load
fi

